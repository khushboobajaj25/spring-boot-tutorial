
1. What are the alternatives of Hibernates?
2. Do we use JPA in real world industry? because based on my understanding if query is very complex then it is very hard to maintian through JPA
3. ORM vs JDBC speed while executing CRUD queries also when some complex queries?
4. How Detached Lifecycle works, I am not able understand when it get detached?
5. In Persistent we have saveUser so we save email after that I have not called save function so it will save email in db? or also if I return saveUser to another function, that function modify the values in saveUser will it also update the database?
6. @Entity and @Table are optional anyone annonation will work to create table? and what is the difference between them?
7. What do you mean by Removed Lifecycle? what it means "Marked for Removal" will it get remove automaically ?
