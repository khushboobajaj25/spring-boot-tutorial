-- Schema initialization script
-- This file runs before Hibernate creates tables (if needed)
-- Useful for custom SQL that needs to run first

-- Example: Create custom sequences or functions
-- CREATE SEQUENCE IF NOT EXISTS user_seq START WITH 1 INCREMENT BY 1;

-- Note: With spring.jpa.hibernate.ddl-auto=create-drop, 
-- Hibernate will handle table creation, so this is optional
