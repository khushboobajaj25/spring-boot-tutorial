-- Sample data initialization script
-- This file runs AFTER tables are created
-- Great for inserting test data during development

-- Note: We'll add actual INSERT statements later when we create entities
-- For now, this demonstrates the concept

-- Example (will work after we create User entity):
-- INSERT INTO users (id, name, email, age) VALUES (1, 'John Doe', 'john@example.com', 30);
-- INSERT INTO users (id, name, email, age) VALUES (2, 'Jane Smith', 'jane@example.com', 25);
